{
    "layer": "top",
    "position": "top",
    "output": "eDP-1",
    "height": 35,
    "spacing": 15,
    "margin-top": 4,
    "margin-left": 8,
    "margin-right": 8,

    "modules-left": ["network", "bluetooth", "custom/window-count", "niri/window"],
    "modules-center": ["clock"],
    "modules-right": ["wireplumber", "memory", "battery", "custom/battery-health"],

    // Modules in left-to-right order

    "network": {
        "format-wifi": " <span color='#F5F5DC'>{essid}</span>",
        "format-ethernet": " <span color='#F5F5DC'>{ifname}</span>",
        "format-disconnected": " <span color='#F5F5DC'>No Network</span>",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}",
        "on-click": "foot ~/.config/waybar/scripts/network_script.sh"
    },

    "bluetooth": {
        "format": " <span color='#F5F5DC'>{status}</span>",
        "format-connected": " <span color='#F5F5DC'>{device_alias}</span>",
        "format-connected-battery": " <span color='#F5F5DC'>{device_alias} {device_battery_percentage}%</span>",
        "tooltip-format": "{controller_alias}\t{controller_address}\n\n{num_connections} connected",
        "tooltip-format-connected": "{controller_alias}\t{controller_address}\n\n{num_connections} connected\n\n{device_enumerate}",
        "tooltip-format-enumerate-connected": "{device_alias}\t{device_address}",
        "tooltip-format-enumerate-connected-battery": "{device_alias}\t{device_address}\t{device_battery_percentage}%",
        "on-click": "blueman-manager"
    },

    "custom/window-count": {
        "exec": "~/.config/waybar/scripts/window-count.sh",
        "interval": 1,
        "format": "{}",
        "tooltip": false,
        "markup": true
    },

    "niri/window": {
        "format": " <span color='#F5F5DC'>{app_id}</span>"
    },

    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%Y-%m-%d %H:%M:%S}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "memory": {
        "interval": 10,
        "states": {
            "warning": 51,
            "critical": 76
        },
        "format": " <span color='#F5F5DC'>{percentage}%</span>",
        "tooltip-format": "RAM: {percentage}%\nUsed: {used:0.2f}G\nAvailable: {avail:0.2f}G",
    },

    "wireplumber": {
        "format": "{icon} <span color='#F5F5DC'>{volume}%</span>",
        "format-muted": "",
        "format-icons": ["", "", ""],
        "on-click": "pavucontrol"
    },

    "custom/battery-health": {
        "exec": "~/.config/waybar/scripts/battery-health.sh",
        "interval": "once",
        "format": "{}",
        "tooltip": false
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} <span color='#F5F5DC'>{capacity}%</span>",
        "format-charging": " <span color='#F5F5DC'>{capacity}%</span>",
        "format-plugged": " <span color='#F5F5DC'>{capacity}%</span>",
        "format-icons": ["", "", "", ""]    }
}
